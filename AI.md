# A-AiAssisted
1. Used Claude to generate template for Command.java abstract class. Was able to use it for other commands well.
2. Used Claude to generate ErrorDialog css labels and setError (similar to dialogbox.flip) but to set the dialog to red color to highlight error.